The method Trie.find() traverses through the prefix String and searches through the levels of the Trie until the last chararcter of prefix und returns the corresponding dictionary.

Trienode.suffixes() 